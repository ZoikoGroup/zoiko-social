/**
 * market-nutrition-supplies design tokens.
 *
 * The same palette as the other market landing pages. Duplicated rather than
 * imported so each route stays independent.
 */
export const C = {
  /** Headings, product names and body text. */
  ink: "#073B47",
  /** Primary brand teal: buttons, prices, tags and the active category. */
  brand: "#066879",
  /** Secondary copy and meta lines. */
  muted: "#5B7178",
  /** Hairline borders on cards, inputs and dividers. */
  line: "#DCEAEE",
  /** Tag fill, badge fill and the confidence panel. */
  chip: "#EEF8F9",
  /** Product-listing section background. */
  panel: "#F7F9FA",
  /** Category eyebrows and featured badges. */
  warm: "#E88924",
} as const;
